<% if current_user.articles.count >= session[:m_posting] && !current_user.admin? %>
    <br><br><br><br><br><br><br><br><br>
    <h4 align="center"><%= "You have exceeded the number of ads permitted on your account!" %></h4>
    <br><br><br><br><br><br><br><br><br>
<% else %>
  <% if session[:mobile] == "Y" %>
    <div align = "left" class='article-title col-xs-6 gutter-0'>
      <br><% session[:step2] = "Insert a New Ad" %>
      Insert a New Ad  
    </div>
    <div align = "right" class='article-title col-xs-6 gutter-0'>
      <br><%= current_user.articles.count+1 %> of <%= session[:m_posting] %>   
    </div>
    <%= render 'mmform' %> 
  <% else %>
    <h4 align="left">Insert a New Ad - No. <%= current_user.articles.count+1 %> of <%= session[:m_posting] %></h4>
    <%= render 'form' %>
  <% end %>
<% end %>